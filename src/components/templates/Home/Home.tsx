import ActivityList from '../../organisms/ActivityListContainer/ActivityListContainer';
import Layout from '../Layout/Layout';

const Home = () => {
  return (
    <div>
      <Layout>
        <ActivityList />
      </Layout>
    </div>
  );
};

export default Home;
